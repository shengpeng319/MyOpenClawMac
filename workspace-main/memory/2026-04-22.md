# 2026-04-22 Daily Memory

## 日期: 2026-04-22 (周三)

## 凌晨更新（00:03）
- Jarvis 继续心跳维护
- 昨日（4/21周二）全天正常：早安问候、课程提醒、进化报告均已执行

---

## 早安问候 Cron 失败（07:05）
- 错误：`HTTP 500 api_error: your current token plan not support model, MiniMax-M2.7-highspeed (2061)`
- **需检查 OpenClaw 模型配置，MiniMax token plan 可能过期或不支持 M2.7-highspeed**

---

## 晚间补充（22:00 进化报告）

### 今日关键事件

1. **07:05 全局 Cron 失败** — 所有定时任务（早安问候、课程提醒、金融研报）报 `token plan not support model MiniMax-M2.7-highspeed`，根本原因：MiniMax API Key 耗尽/过期
2. **07:10–07:13 批量更新 Key** — Peng 提供新 key，我定位到 `~/.openclaw/agents/<agent>/agent/auth-profiles.json`，用 sed 批量替换 5 个子 Agent 的旧 key，全部成功
3. **07:13 Hermes Key 查找未完成** — 请求中途被 abort，未给出最终答案（Hermes 是独立 OpenClaw 实例，config 在 `~/Library/Application Support/OpenClaw/` 或其他路径，待下次确认）
4. **07:31 "llm wiki" 歧义** — Peng 说"llm wiki"，我找不到文件，主动给出3个选项让他澄清（正确的做法）
5. **14:00 Gateway 重启路径问题** — `openclaw` 命令不在 PATH，我绕了3步才找到 `~/.nvm/versions/node/v24.14.1/bin/openclaw`

### 学会什么
- `openclaw` CLI 完整路径：`~/.nvm/versions/node/v24.14.1/bin/openclaw`
- 所有 Agent 的 MiniMax key 集中在 `~/.openclaw/agents/<name>/agent/auth-profiles.json`
- 批量替换 Key 可用 `sed -i ''` 一次搞定多个文件
- 遇到歧义指令时，主动给选项比猜测更高效

### 犯的错
- "llm wiki" 没有一次理解用户意图，虽然后来补救问了选项，但可以在搜索 workspace 无果后立即问
- Hermes key 查找没有完成就被 abort，说明大范围 `find ~` 可能太慢

### 待固化技能
1. **openclaw 路径备忘**：写到 TOOLS.md 或 AGENTS.md，常驻
2. **Hermes 实例定位**：下次快速定位 Hermes config 路径（`~/Library/Application Support/OpenClaw/`）
3. **Key 轮换 SOP**：当 key 报 401/403 时，自动检测所有 agent 的 auth-profiles.json 并报告哪个需要更新

### Token/Cost
- main session: 51,379 tokens ($0.046)
- 今日 crons 全部失败（token plan 问题），已通过 key 更新修复


---

## 进入安静时段（23:03）
- 23:00-08:00 安静时段开始
- 全天心跳正常
