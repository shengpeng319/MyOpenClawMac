# 📊 每日进化报告 - 2026-02-26

## 📋 今日会话回顾

### 1. Agent 配置与管理
- 检查当前 agent 数量：1 个 (main)
- 配置新 agent "coder"（飞书多通道接入）

### 2. 飞书多通道配置
- 尝试为 coder、projectmanager、researcher 配置独立的飞书机器人
- 使用 `channels.feishu.accounts` 配置多账户
- 配置 bindings 实现路由

### 3. 飞书插件升级
- 发现飞书插件版本过旧（0.1.7），不支持 accounts 字段
- 升级 OpenClaw 到 2026.2.25，内置飞书插件更新

### 4. 群消息问题排查
- 用户反馈群里 @agent 能收到，不 @ 收不到
- 检查 `requireMention: false` 配置
- 多次重启 Gateway 尝试修复
- **最终结论**：群消息根本没到达 Gateway，可能是飞书后台事件订阅配置问题

### 5. 浏览器 Relay 问题
- 检查 Browser Relay 状态
- 确认插件正常，只是需要用户手动 attach

---

## 🧠 学到的要点

1. **飞书插件 accounts 配置**：需要 OpenClaw >= 2026.2.13 才能使用
2. **群消息路由**：日志中 `(p2p)` 表示私聊，群消息应有不同标识
3. **配置优先级**：`groups."*" ` 通配符应该能匹配所有群，但实际可能未生效

---

## ⚠️ 遇到的问题

1. **飞书插件多账户不支持**：旧版本不支持 accounts 字段
2. **群消息不工作**：@ 能收到，不 @ 收不到
3. **bot open_id 解析失败**：coder/researcher/projectmanager 的 bot open_id 显示为 unknown

---

## 💡 建议固化的技能

1. **飞书多通道排查流程**：
   - 检查日志中的 `(p2p)` vs `(group)` 标识
   - 确认 bot open_id 是否正确解析
   - 验证飞书后台事件订阅配置

---

## 🔧 后续行动

1. 继续排查群消息问题
2. 确认所有机器人都已加入群聊
3. 检查飞书后台权限配置
